name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create release archive
        run: |
          cd custom_components
          zip -r ../schwoerer_wgt_controller.zip schwoerer_wgt_controller/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: schwoerer_wgt_controller.zip
          generate_release_notes: true
          draft: false
          prerelease: false
